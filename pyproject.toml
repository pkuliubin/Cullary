[project]
name = "cullary"
version = "0.1.0"
description = "Local-first photo culling and preprocessing toolkit."
requires-python = ">=3.11"

[project.scripts]
cullary-preprocess = "cullary.preprocessing.cli:main"

[tool.setuptools.packages.find]
where = ["src"]
